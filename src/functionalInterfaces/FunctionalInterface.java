package functionalInterfaces;

// Functional interface are interfaces which only contains one method
// These are useful with lambda expressions

@java.lang.FunctionalInterface
public interface FunctionalInterface {
	int interfaceMethod(int age);
}
